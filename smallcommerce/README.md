# Small Commerce Backend

A simple backend for a small e-commerce application using Spring Boot + MySQL.

## Features
- CRUD for Users, Products, Orders
- Modular architecture with Controllers, Services, Repositories
- Validation with Spring Boot

## Run
1. Create MySQL database `small_commerce_db`
2. Update `application.properties` with your credentials
3. Run:
```bash
mvn spring-boot:run